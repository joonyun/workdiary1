
<div class="col-md-8" >
<%= simple_form_for (@survey) do |f| %>
  <% if @survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey.errors.count, "error") %> prohibited this survey from being saved:</h2>

      <ul>
      <% @survey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

      <%= f.input :title, :label => "제목", input_html: { class: "form-control" }  %>

   <% @cnt = 1 %>
   <div class="col-md-8" >
   <%= f.simple_fields_for :questions, @survey.questions do | q | %>
   <%= render :partial => "question_fields", locals: { :f => q }  %>
   <% end%>
    <%= link_to_add_fields '질문추가', f , :questions  %>
   </div>

  <div class="col-md-8">
    <div class="col-md-12">
    <%= f.button :submit, input_html: { class: "btn btn-success" } %>
    <%= link_to '목록', surveys_path, :class => "btn btn-success" %>
    </div>
  </div>
<% end %>

</div>